import 'package:flutter/material.dart';
class TwitterSigning extends StatefulWidget {
  const TwitterSigning({Key? key}) : super(key: key);

  @override
  State<TwitterSigning> createState() => _TwitterSigningState();
}

class _TwitterSigningState extends State<TwitterSigning> {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
