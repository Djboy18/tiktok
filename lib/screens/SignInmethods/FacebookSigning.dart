import 'package:flutter/material.dart';
class FacebookSigning extends StatefulWidget {
  const FacebookSigning({Key? key}) : super(key: key);

  @override
  State<FacebookSigning> createState() => _FacebookSigningState();
}

class _FacebookSigningState extends State<FacebookSigning> {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
