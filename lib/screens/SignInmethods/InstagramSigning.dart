import 'package:flutter/material.dart';
class InstagramSigning extends StatefulWidget {
  const InstagramSigning({Key? key}) : super(key: key);

  @override
  State<InstagramSigning> createState() => _InstagramSigningState();
}

class _InstagramSigningState extends State<InstagramSigning> {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
